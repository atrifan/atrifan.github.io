# Workflow YAML Rules

> This document defines the YAML schema for automation workflows.
> Use this as a system prompt or IDE rules file for AI-assisted workflow creation.

## Overview

Workflows are defined in YAML format. The YAML is the **source of truth** - Mermaid diagrams are generated from it for visualization.

## Basic Structure

```yaml
id: my_workflow              # Auto-generated from name if not provided
name: "My Workflow"
description: "What this workflow does"
version: 1

trigger:
  type: manual  # or cron, webhook, event

inputs:
  - name: query
    type: string
    required: true
    description: "Search query"

steps:
  - id: step1
    tool: connector.tool_name
    params:
      param1: "value"
      param2: "{{query}}"
    output: result

constraints:
  maxToolCalls: 50
  timeout: "5m"
```

## Workflow ID

The `id` field is a unique identifier for the workflow in snake_case format.

### Naming Convention

| Name | Generated ID |
|------|--------------|
| `"Birthday Checker"` | `birthday_checker` |
| `"My Awesome Workflow!"` | `my_awesome_workflow` |
| `"Send Email - Daily Report"` | `send_email_daily_report` |
| `"API v2 Integration"` | `api_v2_integration` |

### Rules

1. **Auto-generated**: If `id` is not provided, it's generated from `name`
2. **Lowercase**: All characters are lowercased
3. **Snake case**: Spaces and hyphens become underscores
4. **Alphanumeric only**: Special characters are removed
5. **No leading/trailing underscores**: Trimmed automatically

### Example

```yaml
# These are equivalent:
id: birthday_checker
name: "Birthday Checker"

# Or just provide name (id auto-generated):
name: "Birthday Checker"
# id will be: birthday_checker
```

## Trigger Types

There are only 3 trigger types. The UI may show "pretty" schedule options (daily, weekly, etc.) but these all convert to cron expressions.

| Type | Description | Use Case |
|------|-------------|----------|
| `manual` | Triggered by user action in UI | On-demand tasks |
| `cron` | Scheduled execution | Recurring tasks (daily, weekly, etc.) |
| `webhook` | Triggered by external HTTP POST | External integrations, callbacks |

### Manual Trigger
```yaml
trigger:
  type: manual
```
Executed when user clicks "Run" in the UI or via API.

### Webhook Trigger
```yaml
trigger:
  type: webhook
```
Triggered via POST to `/api/ai/automations/{automation_id}/hook/{api_key}`.

### Cron Trigger
```yaml
trigger:
  type: cron
  schedule: "0 9 * * *"  # 9 AM daily
  timezone: "America/New_York"
```

**Trigger Fields:**
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | string | Yes | Must be `"cron"` |
| `schedule` | string | Yes | Cron expression (5 fields) |
| `timezone` | string | No | IANA timezone (default: UTC) |

**Cron Format:** `minute hour day-of-month month day-of-week`

| Position | Field | Values | Special Characters |
|----------|-------|--------|-------------------|
| 1 | Minute | 0-59 | `*` `,` `-` `/` |
| 2 | Hour | 0-23 | `*` `,` `-` `/` |
| 3 | Day of Month | 1-31 | `*` `,` `-` `/` |
| 4 | Month | 1-12 | `*` `,` `-` `/` |
| 5 | Day of Week | 0-6 (Sun=0) | `*` `,` `-` `/` |

**Special Characters:**
- `*` = any value
- `*/n` = every n units (e.g., `*/5` = every 5)
- `n,m` = specific values (e.g., `1,15` = 1st and 15th)
- `n-m` = range (e.g., `1-5` = Monday to Friday)

**Common Schedules:**
| Schedule | Cron Expression |
|----------|-----------------|
| Every minute | `* * * * *` |
| Every 5 minutes | `*/5 * * * *` |
| Every 15 minutes | `*/15 * * * *` |
| Every 30 minutes | `*/30 * * * *` |
| Hourly | `0 * * * *` |
| Every 2 hours | `0 */2 * * *` |
| Daily at midnight | `0 0 * * *` |
| Daily at 9 AM | `0 9 * * *` |
| Daily at 6 PM | `0 18 * * *` |
| Weekdays at 9 AM | `0 9 * * 1-5` |
| Weekends at 10 AM | `0 10 * * 0,6` |
| Weekly (Monday 9 AM) | `0 9 * * 1` |
| Bi-weekly (Mon/Thu 9 AM) | `0 9 * * 1,4` |
| Monthly (1st at midnight) | `0 0 1 * *` |
| Monthly (15th at noon) | `0 12 15 * *` |
| Quarterly (1st of Jan/Apr/Jul/Oct) | `0 0 1 1,4,7,10 *` |
| Yearly (Jan 1 at midnight) | `0 0 1 1 *` |

**Common Timezones:**
| Region | Timezone |
|--------|----------|
| US Eastern | `America/New_York` |
| US Central | `America/Chicago` |
| US Mountain | `America/Denver` |
| US Pacific | `America/Los_Angeles` |
| UK | `Europe/London` |
| Central Europe | `Europe/Paris` |
| India | `Asia/Kolkata` |
| Japan | `Asia/Tokyo` |
| Australia | `Australia/Sydney` |
| UTC | `UTC` |

### Webhook Trigger
```yaml
trigger:
  type: webhook
  webhook:
    method: POST
    auth:
      type: api_key
      apiKey:
        header: X-API-Key
    inputSchema:
      type: object
      properties:
        orderId:
          type: string
      required: [orderId]
```

### Event Trigger
```yaml
trigger:
  type: event
  event:
    name: "order.created"
    filter: "event.data.amount > 100"
```

### Automation Trigger
Triggered when another automation completes or reaches a specific step.

```yaml
trigger:
  type: automation
  from: "Order Processing"  # Name of triggering automation
  on: complete  # or step
  # step: processPayment  # If on: step
```

## Required Inputs

Pre-configure required input values. Fields can be:
- **Pre-filled** with actual values
- **Marked sensitive** for vault storage
- **Set to human_input** for runtime collection

```yaml
required_inputs:
  api_key:
    value: "sk-..."
    sensitive: true  # Stored in vault, masked in UI

  customer_email:
    human_input: true  # Requested at runtime
    description: "Customer email for notification"
    type: string

  threshold:
    value: 100
    type: number
```

**Missing Required Inputs:** If a tool step requires an input that isn't available in context or pre-configured, the workflow pauses and sends a notification (email/slack) requesting the input. This is automatic human-in-the-loop error handling.

## Outputs

Workflows deliver results via notifications, not JSON returns. Configure outputs:

```yaml
outputs:
  - type: email
    to: "{{customer_email}}"
    subject: "Order {{orderId}} Processed"
    body: "Your order has been processed. Summary: {{summary}}"

  - type: slack
    channel: "#orders"
    message: "Order {{orderId}} completed: {{summary}}"

  - type: webhook
    url: "https://api.example.com/callback"
    method: POST
    body:
      orderId: "{{orderId}}"
      status: "completed"

  - type: automation
    name: "Send Invoice"  # Trigger another automation
    inputs:
      orderId: "{{orderId}}"
      amount: "{{total}}"
```

## Step Types

### Tool Call Step
Calls an MCP tool from a connector.

```yaml
- id: search
  tool: brave-search.web_search
  params:
    query: "{{userQuery}}"
    count: 10
  output: searchResults
  onError: continue  # or fail, retry
```

### Code Step
Inline JavaScript/TypeScript expression.

```yaml
- id: filter
  code: |
    searchResults.filter(r => r.score > 0.7).slice(0, 5)
  output: topResults
```

### LLM Step
Calls an AI model.

```yaml
- id: summarize
  llm:
    model: "openai/gpt-4o-mini"
    system: "You are a helpful assistant"
    prompt: "Summarize these results: {{topResults}}"
    format: text  # or json
  output: summary
```

For structured output:
```yaml
- id: extract
  llm:
    prompt: "Extract entities from: {{text}}"
    format: json
    schema:
      type: object
      properties:
        entities:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              type: { type: string }
  output: entities
```

### Conditional Step
```yaml
- id: checkEmpty
  if: topResults.length === 0
  then:
    - id: noResults
      return: { status: "empty", message: "No results found" }
  else:
    - id: processResults
      code: topResults.map(r => r.title)
      output: titles
```

### For Loop Step
```yaml
- id: processEach
  for: item in items
  do:
    - id: processItem
      tool: processor.process
      params:
        data: "{{item}}"
      output: processed
```

### While Loop Step
```yaml
- id: pollUntilReady
  while: status !== 'ready'
  maxIterations: 10
  do:
    - id: checkStatus
      tool: api.get_status
      params:
        id: "{{jobId}}"
      output: status
    - id: wait
      code: await new Promise(r => setTimeout(r, 1000))
      output: _
```

### Human-in-the-Loop Step
```yaml
- id: approval
  human:
    message: "Approve this action? Amount: ${{amount}}"
    type: confirm  # or text, choice
    choices: ["Approve", "Reject", "Escalate"]
    timeout: 3600  # seconds
    notify: [email, slack]
  output: decision
```

### Notify Step
```yaml
- id: alert
  notify:
    channels: [email, slack, push]
    message: "Order {{orderId}} processed successfully"
    priority: high  # or low, normal
```

### Return Step
```yaml
- id: done
  return:
    status: "success"
    data: "{{processedData}}"
    timestamp: "{{new Date().toISOString()}}"
```

### Trigger Automation Step
Trigger another automation from within a workflow.

```yaml
- id: startInvoicing
  trigger_automation:
    name: "Generate Invoice"
    inputs:
      orderId: "{{orderId}}"
      amount: "{{total}}"
    wait: false  # Don't wait for completion (default)
  output: invoiceExecution  # Execution ID if wait=false
```

With wait (synchronous):
```yaml
- id: getApproval
  trigger_automation:
    name: "Manager Approval"
    inputs:
      request: "{{request}}"
    wait: true  # Wait for completion
  output: approvalResult
```

### Delay Step
Pause execution for a specified number of seconds.

```yaml
- id: waitBeforeRetry
  delay: 5  # Wait 5 seconds
```

Use cases:
- Rate limiting between API calls
- Waiting for external processes
- Polling intervals

### Set Variable Step
Explicitly set a variable value. Useful for initializing counters, flags, or computed values.

```yaml
- id: initCounter
  set: counter
  value: 0

- id: setFlag
  set: isProcessed
  value: true

- id: computeValue
  set: fullName
  value: "{{firstName}} {{lastName}}"
```

### Stop Step
Stop execution immediately. Can be triggered programmatically or used to handle cancellation.

```yaml
- id: cancelExecution
  stop:
    reason: "User requested cancellation"
    status: cancelled  # or completed (default: cancelled)
```

Use cases:
- Graceful cancellation from external hooks
- Early termination on critical errors
- User-initiated stops

### Wait For Variable Step
Pause execution until a variable is set via external PUT request. Useful for waiting on external callbacks, approvals, or long-running processes.

```yaml
- id: waitForApproval
  wait_for:
    variable: approvalStatus  # Variable to wait for
    timeout: 3600  # Timeout in seconds (default: 3600 = 1 hour)
    pollInterval: 5  # Poll interval in seconds (default: 5)
    condition: "approvalStatus === 'approved'"  # Optional condition
  output: approvalResult
```

**Fields:**
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `variable` | string | Yes | Variable name to wait for |
| `timeout` | number | No | Timeout in seconds (default: 3600) |
| `pollInterval` | number | No | Poll interval in seconds (default: 5) |
| `condition` | string | No | JS condition to check (e.g., `"status === 'done'"`) |

**External Variable Update:**
External systems can set variables via PUT request:
```
PUT /api/ai/automations/{automation_id}/executions/{run_id}/variables
Content-Type: application/json

{
  "variables": {
    "approvalStatus": "approved",
    "approvedBy": "manager@example.com"
  }
}
```

Use cases:
- Waiting for external webhook callbacks
- Human approval workflows
- Long-running external process completion
- Polling for third-party API status changes

## Variable References

Use `{{variable}}` syntax to reference variables:

```yaml
params:
  # Simple variable
  query: "{{userInput}}"

  # Nested property
  name: "{{user.profile.name}}"

  # Array access
  first: "{{items[0]}}"

  # Expression
  count: "{{items.length}}"

  # Template string
  message: "Hello {{user.name}}, you have {{items.length}} items"
```

## Inputs

Define workflow inputs for webhooks and manual triggers:

```yaml
inputs:
  - name: query
    type: string
    required: true
    description: "Search query"

  - name: limit
    type: number
    required: false
    default: 10

  - name: options
    type: object
    required: false
    default: { includeMetadata: true }
```

**Types:** `string`, `number`, `boolean`, `object`, `array`

## Constraints

```yaml
constraints:
  # Maximum tool calls per execution
  maxToolCalls: 50

  # Execution timeout
  timeout: "5m"  # or "60s", "1h"

  # Tools requiring human approval
  requireApprovalFor:
    - "payment.charge"
    - "email.send_bulk"
```

## Error Handling

Each step can specify error behavior:

```yaml
- id: riskyStep
  tool: external.api_call
  params: { ... }
  output: result
  onError: continue  # continue to next step

- id: criticalStep
  tool: payment.charge
  params: { ... }
  output: receipt
  onError: fail  # stop workflow (default)

- id: flakeyStep
  tool: network.request
  params: { ... }
  output: response
  onError: retry  # retry with backoff
```

## Complete Example

```yaml
name: "Daily News Digest"
description: "Fetches news, summarizes, and sends email digest"
version: 1

trigger:
  type: cron
  schedule: "0 8 * * *"
  timezone: "America/New_York"

inputs:
  - name: topics
    type: array
    default: ["AI", "Technology", "Business"]

steps:
  - id: fetchNews
    tool: brave-search.web_search
    params:
      query: "{{topics.join(' OR ')}} news today"
      count: 20
    output: articles

  - id: filterRecent
    code: |
      articles.filter(a => {
        const date = new Date(a.publishedAt);
        const yesterday = new Date(Date.now() - 86400000);
        return date > yesterday;
      })
    output: recentArticles

  - id: checkEmpty
    if: recentArticles.length === 0
    then:
      - id: noNews
        notify:
          channels: [slack]
          message: "No recent news found for topics: {{topics}}"

      - id: exitEarly
        return: { status: "no_news" }

  - id: summarize
    llm:
      model: "openai/gpt-4o-mini"
      prompt: |
        Create a brief digest of these news articles:
        {{JSON.stringify(recentArticles, null, 2)}}

        Format as HTML email with sections per topic.
      format: text
    output: digest

  - id: sendEmail
    tool: email.send
    params:
      to: "{{process.env.DIGEST_EMAIL}}"
      subject: "Daily News Digest - {{new Date().toLocaleDateString()}}"
      html: "{{digest}}"
    output: emailResult

  - id: complete
    return:
      status: "success"
      articleCount: "{{recentArticles.length}}"
      sentTo: "{{process.env.DIGEST_EMAIL}}"

constraints:
  maxToolCalls: 30
  timeout: "2m"
```

---

## Automation Execution API

### Authentication

All execution endpoints support two authentication methods:

1. **Clerk Session** (for UI/browser calls) - Automatic via cookies
2. **API Key** (for external/programmatic calls):
   - `Authorization: Bearer <api_key>` header
   - `X-API-Key: <api_key>` header

The API key is validated by hashing and looking up in the `api_keys` table, then verifying the user owns the automation.

### API Endpoints Overview

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| POST | `/api/ai/automations/{id}/hook/{api_key}` | API key in path | Start automation (webhook) |
| GET | `/api/ai/automations/{id}/executions` | Session/API key | List all executions |
| GET | `/api/ai/automations/{id}/executions/{instance_id}` | Session/API key | Get execution status |
| DELETE | `/api/ai/automations/{id}/executions/{instance_id}` | Session/API key | Stop/cancel execution |
| DELETE | `/api/ai/automations/{id}/executions/{instance_id}?hard=true` | Session/API key | Delete execution |
| POST | `/api/ai/automations/{id}/executions/{instance_id}/input` | Session/API key | Submit input to unblock |
| PUT | `/api/ai/automations/{id}/executions/{instance_id}/variables` | Session/API key | Update variables |
| GET | `/api/ai/automations/{id}/executions/{instance_id}/variables` | Session/API key | Get current variables |
| GET | `/api/ai/automations/{id}/executions/{instance_id}/logs` | Session/API key | Get execution logs |

### Start Automation (Webhook)

```
POST /api/ai/automations/{automation_id}/hook/{api_key}
Content-Type: application/json

{
  "inputs": {
    "field1": "value1",
    "field2": "value2"
  }
}
```

**Response (success):**
```json
{
  "execution_id": "instance-uuid",
  "status": "running",
  "message": "Automation triggered successfully"
}
```

**Response (waiting for input):**
```json
{
  "execution_id": "instance-uuid",
  "status": "waiting_input",
  "message": "Automation requires input before proceeding",
  "input_url": "/automation/{automation_id}/running/{instance_id}/input",
  "missing_inputs": [
    {"name": "customer_email", "type": "string", "description": "Customer email"}
  ]
}
```

### Stop Execution

```
DELETE /api/ai/automations/{automation_id}/executions/{instance_id}
```

Sets status to `cancelled`. The executor polls for this and stops processing.

**Response:**
```json
{
  "success": true,
  "execution": { "id": "instance-uuid", "status": "cancelled" }
}
```

### Submit Input (Unblock waiting_input)

When an execution is in `waiting_input` status, submit the required inputs to resume:

```bash
curl -X POST "https://yourapp.com/api/ai/automations/{automation_id}/executions/{instance_id}/input" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "inputs": {
      "customer_email": "user@example.com",
      "approved": true
    }
  }'
```

**Response:**
```json
{
  "success": true,
  "execution": { "id": "instance-uuid", "status": "running" },
  "message": "Inputs received, execution resumed"
}
```

### Update Variables (for wait_for step)

When an execution is polling for a variable (via `wait_for` step), set the variable externally:

```bash
curl -X PUT "https://yourapp.com/api/ai/automations/{automation_id}/executions/{instance_id}/variables" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "variables": {
      "approvalStatus": "approved",
      "approvedBy": "manager@example.com"
    }
  }'
```

**Response:**
```json
{
  "success": true,
  "execution": { "id": "instance-uuid", "status": "running" },
  "variables": { "approvalStatus": "approved", "approvedBy": "manager@example.com" }
}
```

### Get Current Variables

Check current execution variables:

```bash
curl -X GET "https://yourapp.com/api/ai/automations/{automation_id}/executions/{instance_id}/variables" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### UI Input Page

Users can provide input via web form at:
```
/automation/{automation_id}/running/{instance_id}/input
```

This page shows:
- Automation name and status
- List of required inputs with descriptions
- Form fields for each input
- Timeout countdown (5 minutes)
- Submit button

### Internal Calls (Automation-to-Automation)

When triggering from within another automation, use the `x-internal-call: true` header to bypass API key validation.

### Cron Calls

Vercel Cron jobs use `x-cron-secret` header with `CRON_SECRET` env var for authentication.

---

## Human-in-the-Loop & Input Exceptions

When an automation requires input that isn't available:

1. **Execution pauses** with status `waiting_input`
2. **Notifications sent** via configured channels (email, Slack, push)
3. **User receives link** to `/automation/{automation_id}/running/{instance_id}/input`
4. **User provides input** via web form or API
5. **Execution resumes** automatically

### ⏱️ Timeout Behavior

**IMPORTANT:** Executions waiting for user input have a **5-minute timeout**.

| Wait Type | Timeout | Behavior |
|-----------|---------|----------|
| `waiting_input` (missing inputs) | 5 minutes | Exits with `timeout_user_input` error |
| `wait_for` (variable polling) | 5 minutes (default) | Exits with `timeout_wait_for` error |
| `human` step (approval) | 5 minutes | Exits with `timeout_approval` error |

After timeout, the execution status changes to `failed` with an appropriate error message.

### Notification Message Templates

#### Input Required Notification

```
🤖 Automation "{automation_name}" requires your input

Run ID: {execution_id}
Status: Waiting for input

The following fields are needed to continue:
{missing_inputs_list}

⏱️ This request will timeout in 5 minutes.

📝 Provide input via UI:
{input_url}

🔧 Or via API (curl):
curl -X POST "{api_base_url}/api/ai/automations/{automation_id}/executions/{execution_id}/input" \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{
    "inputs": {
      "{field_name}": "{value}"
    }
  }'
```

#### Wait For Variable Notification

```
⏳ Automation "{automation_name}" is waiting for external event

Run ID: {execution_id}
Status: Waiting for variable "{variable_name}"

The automation is polling for variable "{variable_name}" to be set.

⏱️ This will timeout in {timeout_seconds} seconds.

🔧 Set the variable via API:
curl -X PUT "{api_base_url}/api/ai/automations/{automation_id}/executions/{execution_id}/variables" \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{
    "variables": {
      "{variable_name}": {value}
    }
  }'
```

#### Human Approval Notification

```
👤 Automation "{automation_name}" requires your approval

Run ID: {execution_id}
Step: {step_id}

{approval_message}

⏱️ This request will timeout in 5 minutes.

📝 Respond via UI:
{approval_url}

🔧 Or via API:
# Approve
curl -X POST "{api_base_url}/api/ai/automations/{automation_id}/executions/{execution_id}/input" \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"inputs": {"approved": true}}'

# Reject
curl -X POST "{api_base_url}/api/ai/automations/{automation_id}/executions/{execution_id}/input" \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"inputs": {"approved": false, "reason": "Rejected by user"}}'
```

#### Timeout Notification

```
⚠️ Automation "{automation_name}" timed out

Run ID: {execution_id}
Status: Failed
Error: {timeout_type}

The automation was waiting for {wait_description} but no response was received within 5 minutes.

The execution has been marked as failed. You can:
- View logs: {logs_url}
- Retry: {retry_url}
```

### Configuring Input Requirements

```yaml
required_inputs:
  # Pre-filled value (no prompt needed)
  api_key:
    value: "sk-..."
    sensitive: true

  # Always prompt user at runtime
  customer_email:
    human_input: true
    description: "Customer email for notification"
    type: string

  # Optional with default
  threshold:
    value: 100
    type: number
```

---

## Notification Tools

Notification tools are **auto-detected** from your connectors based on tool names and descriptions containing keywords like:
- **Email**: `email`, `mail`, `send_email`
- **Slack**: `slack`, `post_message`, `channel`
- **Push**: `push`, `notification`, `mobile`
- **SMS**: `sms`, `text_message`
- **Webhook**: `webhook`, `http_post`, `callback`

These tools are automatically used for:
- Human-in-the-loop input requests
- Error notifications
- Workflow output delivery

### Using Notifications in Workflows

```yaml
# In outputs section
outputs:
  - type: email
    tool: email-connector.send_email
    to: "{{customer_email}}"
    subject: "Order {{orderId}} Processed"
    body: "{{summary}}"

  - type: slack
    tool: slack-connector.post_message
    channel: "#orders"
    message: "Order {{orderId}} completed"

# In notify step
steps:
  - id: alert
    notify:
      channels: [email, slack]  # Uses detected tools
      message: "Urgent: {{alert_message}}"
      priority: high
```

---

## Execution States

| Status | Description |
|--------|-------------|
| `pending` | Execution created, not yet started |
| `running` | Actively executing steps |
| `waiting_input` | Paused, waiting for human input |
| `paused` | Manually paused by user |
| `completed` | Successfully finished |
| `failed` | Error occurred |
| `cancelled` | Cancelled by user |

---

## Cron Scheduling

> **Note:** Cron triggers are implemented via Vercel Cron Jobs. The cron expression is stored in the automation and Vercel handles the scheduling.

Common cron expressions:
| Schedule | Expression |
|----------|------------|
| Every minute | `* * * * *` |
| Every 5 minutes | `*/5 * * * *` |
| Hourly | `0 * * * *` |
| Daily at 9 AM | `0 9 * * *` |
| Weekly Monday 9 AM | `0 9 * * 1` |
| Monthly 1st at midnight | `0 0 1 * *` |

---

## Available Tools

> The following tools are available from your active connectors.
> Use the format `connector.tool_name` in your workflow steps.

<!-- TOOLS_SECTION_START -->
## Available Tools

The following tools are available from your active connectors. Use the full tool name (server.tool_name) in your workflow steps.

**Total Connectors:** 1 | **Total Tools:** 29

### Default Key
- **Server Name:** `default`
- **Type:** Internal MCP
- **Tools:** 29

#### `default.find_next_eclipse`

**Description:** Find the next upcoming solar or lunar eclipse with visibility info, countdown, and best viewing locations. Supports filtering by eclipse type and checking visibility from a specific location.

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "type": {
      "enum": [
        "solar",
        "lunar",
        "any"
      ],
      "type": "string",
      "description": "Type of eclipse to find: solar (sun blocked by moon), lunar (moon in Earth shadow), or any"
    },
    "latitude": {
      "type": "number",
      "description": "User latitude for visibility check (-90 to 90). Provide with longitude for personalized visibility info."
    },
    "longitude": {
      "type": "number",
      "description": "User longitude for visibility check (-180 to 180). Provide with latitude for personalized visibility info."
    }
  }
}
```

**Parameters:**
- `type` (string): Type of eclipse to find: solar (sun blocked by moon), lunar (moon in Earth shadow), or any
- `latitude` (number): User latitude for visibility check (-90 to 90). Provide with longitude for personalized visibility info.
- `longitude` (number): User longitude for visibility check (-180 to 180). Provide with latitude for personalized visibility info.

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "date": {
      "type": "string",
      "description": "Eclipse date in YYYY-MM-DD format"
    },
    "type": {
      "enum": [
        "solar",
        "lunar"
      ],
      "type": "string",
      "description": "Eclipse type"
    },
    "subtype": {
      "enum": [
        "total",
        "partial",
        "annular",
        "penumbral",
        "hybrid"
      ],
      "type": "string",
      "description": "Eclipse subtype"
    },
    "duration": {
      "type": "string",
      "description": "Duration of totality/maximum phase"
    },
    "daysUntil": {
      "type": "number",
      "description": "Days until the eclipse"
    },
    "magnitude": {
      "type": "number",
      "description": "Eclipse magnitude (0-1+)"
    },
    "coordinates": {
      "type": "object",
      "description": "Greatest eclipse point coordinates {lat, lon}"
    },
    "peakTimeUTC": {
      "type": "string",
      "description": "Peak time in UTC (HH:MM)"
    },
    "visibleRegions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of regions where eclipse is visible"
    },
    "bestVisibleFrom": {
      "type": "string",
      "description": "Best viewing location description"
    },
    "visibilityScore": {
      "type": "string",
      "description": "Visibility quality from provided location"
    },
    "visibleFromLocation": {
      "type": "boolean",
      "description": "Whether visible from provided coordinates"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_find_next_eclipse
    type: action
    name: "Use find_next_eclipse"
    tool: "default.find_next_eclipse"
    input:
      type: "example"
      latitude: 123
      longitude: 123
```

---

#### `default.calculate_savings_plan`

**Description:** Calculate a budget and savings plan. Supports two modes: (1) Goal mode - save until you reach a target amount, (2) Duration mode - save for a specific number of months. Optionally include compound interest from a savings account.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "monthlyIncome",
    "monthlyFixedExpenses",
    "currentSavings",
    "intensity",
    "currency"
  ],
  "properties": {
    "currency": {
      "enum": [
        "USD",
        "EUR",
        "GBP",
        "JPY",
        "RON"
      ],
      "type": "string"
    },
    "intensity": {
      "enum": [
        "light",
        "medium",
        "aggressive"
      ],
      "type": "string",
      "description": "Savings intensity: light (10%), medium (25%), aggressive (40%) of disposable income"
    },
    "savingsGoal": {
      "type": "number",
      "description": "Target savings amount (required if savingsMode is \"goal\")"
    },
    "savingsMode": {
      "enum": [
        "goal",
        "duration"
      ],
      "type": "string",
      "description": "Mode: \"goal\" to reach a target amount, \"duration\" to save for X months"
    },
    "interestRate": {
      "type": "number",
      "description": "Annual interest rate as percentage, e.g., 5 for 5% (optional, requires interestEnabled)"
    },
    "monthlyTaxes": {
      "type": "number",
      "description": "Monthly taxes (set to 0 if using net income)"
    },
    "monthlyIncome": {
      "type": "number",
      "description": "Monthly net income (after taxes)"
    },
    "currentSavings": {
      "type": "number",
      "description": "Current savings amount"
    },
    "interestEnabled": {
      "type": "boolean",
      "description": "Enable compound interest from savings account (optional)"
    },
    "compoundingFrequency": {
      "enum": [
        "yearly",
        "monthly",
        "daily"
      ],
      "type": "string",
      "description": "How often interest compounds (optional, default: yearly)"
    },
    "monthlyFixedExpenses": {
      "type": "number",
      "description": "Fixed monthly expenses (rent, utilities, subscriptions)"
    },
    "savingsDurationMonths": {
      "type": "number",
      "description": "Number of months to save (required if savingsMode is \"duration\")"
    }
  }
}
```

**Parameters:**
- `currency` (string, required): 
- `intensity` (string, required): Savings intensity: light (10%), medium (25%), aggressive (40%) of disposable income
- `savingsGoal` (number): Target savings amount (required if savingsMode is "goal")
- `savingsMode` (string): Mode: "goal" to reach a target amount, "duration" to save for X months
- `interestRate` (number): Annual interest rate as percentage, e.g., 5 for 5% (optional, requires interestEnabled)
- `monthlyTaxes` (number): Monthly taxes (set to 0 if using net income)
- `monthlyIncome` (number, required): Monthly net income (after taxes)
- `currentSavings` (number, required): Current savings amount
- `interestEnabled` (boolean): Enable compound interest from savings account (optional)
- `compoundingFrequency` (string): How often interest compounds (optional, default: yearly)
- `monthlyFixedExpenses` (number, required): Fixed monthly expenses (rent, utilities, subscriptions)
- `savingsDurationMonths` (number): Number of months to save (required if savingsMode is "duration")

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "savingsMode": {
      "type": "string",
      "description": "The savings mode used (goal or duration)"
    },
    "savingsRate": {
      "type": "number",
      "description": "Percentage of disposable income being saved"
    },
    "finalBalance": {
      "type": "number",
      "description": "Final savings balance at end of plan"
    },
    "monthsToGoal": {
      "type": "number",
      "description": "Number of months in the plan"
    },
    "monthlySavings": {
      "type": "number",
      "description": "Amount to save each month"
    },
    "disposableIncome": {
      "type": "number",
      "description": "Monthly disposable income after fixed expenses"
    },
    "totalInterestEarned": {
      "type": "number",
      "description": "Total interest earned (if interest enabled)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_savings_plan
    type: action
    name: "Use calculate_savings_plan"
    tool: "default.calculate_savings_plan"
    input:
      currency: "example"
      intensity: "example"
      savingsGoal: 123
      savingsMode: "example"
      interestRate: 123
      monthlyTaxes: 123
      monthlyIncome: 123
      currentSavings: 123
      interestEnabled: true
      compoundingFrequency: "example"
      monthlyFixedExpenses: 123
      savingsDurationMonths: 123
```

---

#### `default.send_push_notification`

**Description:** Send a push notification to the user's registered devices (browser/mobile). Use this to alert users about automation status, required inputs, errors, or any important updates. The notification will appear in the browser or on mobile devices that have enabled push notifications.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "title",
    "body"
  ],
  "properties": {
    "tag": {
      "type": "string",
      "description": "Tag for grouping notifications - same tag replaces previous (optional)"
    },
    "url": {
      "type": "string",
      "description": "URL to open when notification is clicked (optional)"
    },
    "body": {
      "type": "string",
      "description": "Notification body message (required, max 200 chars)"
    },
    "type": {
      "enum": [
        "automation",
        "input_required",
        "error",
        "success",
        "info"
      ],
      "type": "string",
      "description": "Notification type - determines icon and behavior (default: info)"
    },
    "title": {
      "type": "string",
      "description": "Notification title (required, max 50 chars)"
    },
    "executionId": {
      "type": "string",
      "description": "Associated execution ID for deep linking (optional)"
    },
    "automationId": {
      "type": "string",
      "description": "Associated automation ID for deep linking (optional)"
    },
    "requireInteraction": {
      "type": "boolean",
      "description": "Keep notification visible until user interacts (default: false)"
    }
  }
}
```

**Parameters:**
- `tag` (string): Tag for grouping notifications - same tag replaces previous (optional)
- `url` (string): URL to open when notification is clicked (optional)
- `body` (string, required): Notification body message (required, max 200 chars)
- `type` (string): Notification type - determines icon and behavior (default: info)
- `title` (string, required): Notification title (required, max 50 chars)
- `executionId` (string): Associated execution ID for deep linking (optional)
- `automationId` (string): Associated automation ID for deep linking (optional)
- `requireInteraction` (boolean): Keep notification visible until user interacts (default: false)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "sent": {
      "type": "number",
      "description": "Number of devices notification was sent to"
    },
    "failed": {
      "type": "number",
      "description": "Number of failed deliveries"
    },
    "message": {
      "type": "string",
      "description": "Status message"
    },
    "success": {
      "type": "boolean",
      "description": "Whether notification was sent successfully"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_send_push_notification
    type: action
    name: "Use send_push_notification"
    tool: "default.send_push_notification"
    input:
      tag: "example"
      url: "example"
      body: "example"
      type: "example"
      title: "example"
      executionId: "example"
      automationId: "example"
      requireInteraction: true
```

---

#### `default.calculate_position_size`

**Description:** Calculate trading position size based on risk management. Supports 4 modes: riskOnly (get suggestions), riskAndSL (calculate quantity), riskAndQty (calculate stop loss), slAndQty (calculate risk %). Direction can be long (buy) or short (sell).

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "mode",
    "capital",
    "entryPrice",
    "direction"
  ],
  "properties": {
    "mode": {
      "enum": [
        "riskOnly",
        "riskAndSL",
        "riskAndQty",
        "slAndQty"
      ],
      "type": "string",
      "description": "Calculation mode: riskOnly (suggest SL/Qty combos from risk%), riskAndSL (calc quantity from risk% + SL), riskAndQty (calc SL from risk% + quantity), slAndQty (calc risk% from SL + quantity)"
    },
    "capital": {
      "type": "number",
      "description": "Total trading capital in currency"
    },
    "quantity": {
      "type": "number",
      "description": "Number of units/shares. Required for riskAndQty, slAndQty modes"
    },
    "direction": {
      "enum": [
        "long",
        "short"
      ],
      "type": "string",
      "description": "Trade direction: long (buy, expecting price to rise) or short (sell, expecting price to fall)"
    },
    "entryPrice": {
      "type": "number",
      "description": "Entry price of the asset"
    },
    "riskPercent": {
      "type": "number",
      "description": "Risk percentage of capital (0-100). Required for riskOnly, riskAndSL, riskAndQty modes. Recommended: 1-2%"
    },
    "stopLossPrice": {
      "type": "number",
      "description": "Stop loss price. Required for riskAndSL, slAndQty modes. Must be below entry for long, above entry for short"
    }
  }
}
```

**Parameters:**
- `mode` (string, required): Calculation mode: riskOnly (suggest SL/Qty combos from risk%), riskAndSL (calc quantity from risk% + SL), riskAndQty (calc SL from risk% + quantity), slAndQty (calc risk% from SL + quantity)
- `capital` (number, required): Total trading capital in currency
- `quantity` (number): Number of units/shares. Required for riskAndQty, slAndQty modes
- `direction` (string, required): Trade direction: long (buy, expecting price to rise) or short (sell, expecting price to fall)
- `entryPrice` (number, required): Entry price of the asset
- `riskPercent` (number): Risk percentage of capital (0-100). Required for riskOnly, riskAndSL, riskAndQty modes. Recommended: 1-2%
- `stopLossPrice` (number): Stop loss price. Required for riskAndSL, slAndQty modes. Must be below entry for long, above entry for short

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "description": "Calculation mode used"
    },
    "quantity": {
      "type": "number",
      "description": "Position quantity/units"
    },
    "stopLoss": {
      "type": "number",
      "description": "Stop loss price"
    },
    "direction": {
      "type": "string",
      "description": "Trade direction"
    },
    "riskLabel": {
      "type": "string",
      "description": "Risk assessment: Low Risk, Moderate Risk, or High Risk"
    },
    "riskAmount": {
      "type": "number",
      "description": "Risk amount in currency"
    },
    "slDistance": {
      "type": "number",
      "description": "Stop loss distance from entry in price"
    },
    "riskPercent": {
      "type": "number",
      "description": "Risk percentage of capital"
    },
    "suggestions": {
      "type": "array",
      "description": "Suggested positions (only in riskOnly mode)"
    },
    "takeProfits": {
      "type": "array",
      "description": "Take profit levels at 1.5:1, 2:1, 3:1 risk:reward ratios"
    },
    "positionValue": {
      "type": "number",
      "description": "Total position value (quantity * entryPrice)"
    },
    "calculatedField": {
      "type": "string",
      "description": "Which field was calculated: suggestions, quantity, stopLoss, or riskPercent"
    },
    "slDistancePercent": {
      "type": "number",
      "description": "Stop loss distance as percentage"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_position_size
    type: action
    name: "Use calculate_position_size"
    tool: "default.calculate_position_size"
    input:
      mode: "example"
      capital: 123
      quantity: 123
      direction: "example"
      entryPrice: 123
      riskPercent: 123
      stopLossPrice: 123
```

---

#### `default.zone_calculator`

**Description:** Convert time between timezones. Supports UTC offsets (e.g., UTC+5, UTC-8) and major city timezones (e.g., America/New_York, Europe/London, Asia/Tokyo). Returns converted times with day change indicators.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "time",
    "fromTimezone",
    "toTimezones"
  ],
  "properties": {
    "time": {
      "type": "string",
      "description": "Time in HH:MM format (24-hour), e.g., \"14:30\""
    },
    "toTimezones": {
      "type": "array",
      "items": {
        "enum": [
          "UTC",
          "UTC-12",
          "UTC-11",
          "UTC-10",
          "UTC-9",
          "UTC-8",
          "UTC-7",
          "UTC-6",
          "UTC-5",
          "UTC-4",
          "UTC-3",
          "UTC-2",
          "UTC-1",
          "UTC+1",
          "UTC+2",
          "UTC+3",
          "UTC+4",
          "UTC+5",
          "UTC+5:30",
          "UTC+5.5",
          "UTC+6",
          "UTC+7",
          "UTC+8",
          "UTC+9",
          "UTC+10",
          "UTC+11",
          "UTC+12",
          "America/New_York",
          "America/Los_Angeles",
          "America/Chicago",
          "America/Denver",
          "America/Toronto",
          "America/Vancouver",
          "America/Mexico_City",
          "America/Sao_Paulo",
          "Europe/London",
          "Europe/Paris",
          "Europe/Berlin",
          "Europe/Rome",
          "Europe/Madrid",
          "Europe/Amsterdam",
          "Europe/Bucharest",
          "Europe/Moscow",
          "Europe/Istanbul",
          "Asia/Dubai",
          "Asia/Kolkata",
          "Asia/Bangkok",
          "Asia/Singapore",
          "Asia/Hong_Kong",
          "Asia/Shanghai",
          "Asia/Tokyo",
          "Asia/Seoul",
          "Australia/Sydney",
          "Australia/Melbourne",
          "Pacific/Auckland"
        ],
        "type": "string"
      },
      "description": "Target timezones to convert to. Use UTC offsets or IANA timezone IDs"
    },
    "fromTimezone": {
      "enum": [
        "UTC",
        "UTC-12",
        "UTC-11",
        "UTC-10",
        "UTC-9",
        "UTC-8",
        "UTC-7",
        "UTC-6",
        "UTC-5",
        "UTC-4",
        "UTC-3",
        "UTC-2",
        "UTC-1",
        "UTC+1",
        "UTC+2",
        "UTC+3",
        "UTC+4",
        "UTC+5",
        "UTC+5:30",
        "UTC+5.5",
        "UTC+6",
        "UTC+7",
        "UTC+8",
        "UTC+9",
        "UTC+10",
        "UTC+11",
        "UTC+12",
        "America/New_York",
        "America/Los_Angeles",
        "America/Chicago",
        "America/Denver",
        "America/Toronto",
        "America/Vancouver",
        "America/Mexico_City",
        "America/Sao_Paulo",
        "Europe/London",
        "Europe/Paris",
        "Europe/Berlin",
        "Europe/Rome",
        "Europe/Madrid",
        "Europe/Amsterdam",
        "Europe/Bucharest",
        "Europe/Moscow",
        "Europe/Istanbul",
        "Asia/Dubai",
        "Asia/Kolkata",
        "Asia/Bangkok",
        "Asia/Singapore",
        "Asia/Hong_Kong",
        "Asia/Shanghai",
        "Asia/Tokyo",
        "Asia/Seoul",
        "Australia/Sydney",
        "Australia/Melbourne",
        "Pacific/Auckland"
      ],
      "type": "string",
      "description": "Source timezone. Use UTC offsets (UTC-5, UTC+8) or IANA timezone IDs (America/New_York, Europe/London, Asia/Tokyo)"
    }
  }
}
```

**Parameters:**
- `time` (string, required): Time in HH:MM format (24-hour), e.g., "14:30"
- `toTimezones` (array, required): Target timezones to convert to. Use UTC offsets or IANA timezone IDs
- `fromTimezone` (string, required): Source timezone. Use UTC offsets (UTC-5, UTC+8) or IANA timezone IDs (America/New_York, Europe/London, Asia/Tokyo)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "sourceCity": {
      "type": "string",
      "description": "Source city name"
    },
    "sourceTime": {
      "type": "string",
      "description": "Original time in HH:MM format"
    },
    "conversions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "city": {
            "type": "string",
            "description": "Target city name"
          },
          "time": {
            "type": "string",
            "description": "Converted time in HH:MM format"
          },
          "offset": {
            "type": "number",
            "description": "UTC offset of target timezone"
          },
          "timezone": {
            "type": "string",
            "description": "Target timezone ID"
          },
          "dayChange": {
            "type": "string",
            "description": "Day change indicator (+1 day, -1 day, or empty)"
          },
          "offsetDiff": {
            "type": "number",
            "description": "Offset difference from source"
          }
        }
      }
    },
    "sourceTimezone": {
      "type": "string",
      "description": "Source timezone ID"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_zone_calculator
    type: action
    name: "Use zone_calculator"
    tool: "default.zone_calculator"
    input:
      time: "example"
      toTimezones: []
      fromTimezone: "example"
```

---

#### `default.calculate_compound_interest`

**Description:** Calculate compound interest growth over time

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "principal",
    "rate",
    "time"
  ],
  "properties": {
    "rate": {
      "type": "number",
      "description": "Annual interest rate (percentage)"
    },
    "time": {
      "type": "number",
      "description": "Time period in years"
    },
    "principal": {
      "type": "number",
      "description": "Initial investment amount"
    },
    "monthlyContribution": {
      "type": "number",
      "description": "Monthly contribution (optional)"
    },
    "compoundingFrequency": {
      "enum": [
        "annually",
        "semi-annually",
        "quarterly",
        "monthly",
        "daily"
      ],
      "type": "string"
    }
  }
}
```

**Parameters:**
- `rate` (number, required): Annual interest rate (percentage)
- `time` (number, required): Time period in years
- `principal` (number, required): Initial investment amount
- `monthlyContribution` (number): Monthly contribution (optional)
- `compoundingFrequency` (string): 

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "finalAmount": {
      "type": "number"
    },
    "effectiveRate": {
      "type": "number"
    },
    "totalInterest": {
      "type": "number"
    },
    "totalContributions": {
      "type": "number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_compound_interest
    type: action
    name: "Use calculate_compound_interest"
    tool: "default.calculate_compound_interest"
    input:
      rate: 123
      time: 123
      principal: 123
      monthlyContribution: 123
      compoundingFrequency: "example"
```

---

#### `default.calculate_age`

**Description:** Calculate age from birthdate with detailed breakdown

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "birthDate"
  ],
  "properties": {
    "birthDate": {
      "type": "string",
      "description": "Birth date in YYYY-MM-DD format"
    }
  }
}
```

**Parameters:**
- `birthDate` (string, required): Birth date in YYYY-MM-DD format

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "days": {
      "type": "number"
    },
    "years": {
      "type": "number"
    },
    "months": {
      "type": "number"
    },
    "totalDays": {
      "type": "number"
    },
    "nextBirthday": {
      "type": "string"
    },
    "daysUntilBirthday": {
      "type": "number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_age
    type: action
    name: "Use calculate_age"
    tool: "default.calculate_age"
    input:
      birthDate: "example"
```

---

#### `default.when_date_info`

**Description:** Get comprehensive information about a date including day of week, zodiac sign, time calculations from today (days, hours, minutes, weeks), and calendar info (day of year, week number, quarter, leap year).

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "date"
  ],
  "properties": {
    "date": {
      "type": "string",
      "description": "Date in YYYY-MM-DD format (required)"
    }
  }
}
```

**Parameters:**
- `date` (string, required): Date in YYYY-MM-DD format (required)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "date": {
      "type": "string",
      "description": "Input date in YYYY-MM-DD format"
    },
    "weeks": {
      "type": "number",
      "description": "Weeks from today (rounded to 1 decimal)"
    },
    "isPast": {
      "type": "boolean"
    },
    "isToday": {
      "type": "boolean"
    },
    "message": {
      "type": "string",
      "description": "Contextual message about the date"
    },
    "quarter": {
      "type": "number",
      "description": "Quarter of year (1-4)"
    },
    "isFuture": {
      "type": "boolean"
    },
    "dayOfWeek": {
      "type": "string",
      "description": "Full day name (e.g., Monday)"
    },
    "dayOfYear": {
      "type": "number",
      "description": "Day of year (1-366)"
    },
    "isLeapYear": {
      "type": "boolean"
    },
    "totalHours": {
      "type": "number",
      "description": "Total hours from today"
    },
    "weekOfYear": {
      "type": "number",
      "description": "Week of year (1-53)"
    },
    "zodiacSign": {
      "type": "string",
      "description": "Zodiac sign for the date"
    },
    "totalMinutes": {
      "type": "number",
      "description": "Total minutes from today"
    },
    "daysFromToday": {
      "type": "number",
      "description": "Days from today (negative = past, positive = future)"
    },
    "formattedDate": {
      "type": "string",
      "description": "Human-readable date (e.g., January 15th, 2026)"
    },
    "dayOfWeekShort": {
      "type": "string",
      "description": "Short day name (e.g., Mon)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_when_date_info
    type: action
    name: "Use when_date_info"
    tool: "default.when_date_info"
    input:
      date: "example"
```

---

#### `default.calculate_tip`

**Description:** Calculate tip amount and total bill. Supports two modes: "static" (provide tipPercentage directly) or "mood" (compute tip from serviceQuality, mood, and budgetSituation). If tipPercentage is not provided, mood mode is used automatically.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "billAmount"
  ],
  "properties": {
    "mood": {
      "enum": [
        "awful",
        "meh",
        "neutral",
        "happy",
        "great"
      ],
      "type": "string",
      "description": "How are you feeling? Used in mood mode. Default: \"neutral\"."
    },
    "billAmount": {
      "type": "number",
      "description": "Bill amount before tip (required)"
    },
    "splitBetween": {
      "type": "number",
      "description": "Number of people to split between (default: 1)"
    },
    "tipPercentage": {
      "type": "number",
      "description": "Tip percentage (e.g., 15, 18, 20). Required for static mode, ignored in mood mode."
    },
    "calculatorMode": {
      "enum": [
        "static",
        "mood"
      ],
      "type": "string",
      "description": "Calculator mode: \"static\" uses tipPercentage directly, \"mood\" computes from feelings. Default: \"static\" if tipPercentage provided, else \"mood\"."
    },
    "serviceQuality": {
      "enum": [
        "terrible",
        "poor",
        "okay",
        "good",
        "amazing"
      ],
      "type": "string",
      "description": "How was the service? Used in mood mode. Default: \"okay\"."
    },
    "budgetSituation": {
      "enum": [
        "very_tight",
        "tight",
        "normal",
        "comfortable",
        "generous"
      ],
      "type": "string",
      "description": "Budget situation. Used in mood mode. Default: \"normal\"."
    }
  }
}
```

**Parameters:**
- `mood` (string): How are you feeling? Used in mood mode. Default: "neutral".
- `billAmount` (number, required): Bill amount before tip (required)
- `splitBetween` (number): Number of people to split between (default: 1)
- `tipPercentage` (number): Tip percentage (e.g., 15, 18, 20). Required for static mode, ignored in mood mode.
- `calculatorMode` (string): Calculator mode: "static" uses tipPercentage directly, "mood" computes from feelings. Default: "static" if tipPercentage provided, else "mood".
- `serviceQuality` (string): How was the service? Used in mood mode. Default: "okay".
- `budgetSituation` (string): Budget situation. Used in mood mode. Default: "normal".

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "total": {
      "type": "number"
    },
    "perPerson": {
      "type": "number"
    },
    "suggested": {
      "type": "boolean"
    },
    "tipAmount": {
      "type": "number"
    },
    "billAmount": {
      "type": "number"
    },
    "splitBetween": {
      "type": "number"
    },
    "tipPercentage": {
      "type": "number"
    },
    "calculatorMode": {
      "type": "string"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_tip
    type: action
    name: "Use calculate_tip"
    tool: "default.calculate_tip"
    input:
      mood: "example"
      billAmount: 123
      splitBetween: 123
      tipPercentage: 123
      calculatorMode: "example"
      serviceQuality: "example"
      budgetSituation: "example"
```

---

#### `default.list_upcoming_eclipses`

**Description:** List upcoming solar and lunar eclipses with dates, visibility info, and countdown. Returns multiple eclipses with filtering options.

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "type": {
      "enum": [
        "solar",
        "lunar",
        "any"
      ],
      "type": "string",
      "description": "Filter by eclipse type: solar, lunar, or any (default)"
    },
    "count": {
      "type": "number",
      "description": "Number of eclipses to return (1-10, default 5)"
    },
    "latitude": {
      "type": "number",
      "description": "User latitude for visibility check (-90 to 90)"
    },
    "longitude": {
      "type": "number",
      "description": "User longitude for visibility check (-180 to 180)"
    }
  }
}
```

**Parameters:**
- `type` (string): Filter by eclipse type: solar, lunar, or any (default)
- `count` (number): Number of eclipses to return (1-10, default 5)
- `latitude` (number): User latitude for visibility check (-90 to 90)
- `longitude` (number): User longitude for visibility check (-180 to 180)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "eclipses": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "List of upcoming eclipses with details (date, type, subtype, peakTimeUTC, duration, magnitude, bestVisibleFrom, visibleRegions, daysUntil, visibleFromLocation, visibilityScore)"
    },
    "totalCount": {
      "type": "number",
      "description": "Total number of eclipses returned"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_list_upcoming_eclipses
    type: action
    name: "Use list_upcoming_eclipses"
    tool: "default.list_upcoming_eclipses"
    input:
      type: "example"
      count: 123
      latitude: 123
      longitude: 123
```

---

#### `default.calculate_countdown`

**Description:** Calculate the number of days, weeks, and months until or since a specific date. Perfect for tracking upcoming events, anniversaries, deadlines, or calculating how long ago something happened.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "eventDate"
  ],
  "properties": {
    "eventDate": {
      "type": "string",
      "description": "Target date in YYYY-MM-DD format. Example: \"2026-12-25\" for Christmas 2026"
    },
    "eventName": {
      "type": "string",
      "description": "Name of the event for display. Example: \"Christmas\", \"My Birthday\", \"Project Deadline\""
    }
  }
}
```

**Parameters:**
- `eventDate` (string, required): Target date in YYYY-MM-DD format. Example: "2026-12-25" for Christmas 2026
- `eventName` (string): Name of the event for display. Example: "Christmas", "My Birthday", "Project Deadline"

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "days": {
      "type": "number",
      "description": "Days until/since event (negative if past)"
    },
    "weeks": {
      "type": "number",
      "description": "Number of complete weeks"
    },
    "isPast": {
      "type": "boolean",
      "description": "Whether the event is in the past"
    },
    "months": {
      "type": "number",
      "description": "Approximate number of months"
    },
    "isToday": {
      "type": "boolean",
      "description": "Whether the event is today"
    },
    "summary": {
      "type": "string",
      "description": "Human-readable summary text"
    },
    "direction": {
      "type": "string",
      "description": "\"until\", \"since\", or \"today\""
    },
    "eventDate": {
      "type": "string",
      "description": "Target date in YYYY-MM-DD format"
    },
    "eventName": {
      "type": "string",
      "description": "Name of the event"
    },
    "absoluteDays": {
      "type": "number",
      "description": "Absolute number of days (always positive)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_countdown
    type: action
    name: "Use calculate_countdown"
    tool: "default.calculate_countdown"
    input:
      eventDate: "example"
      eventName: "example"
```

---

#### `default.send_email`

**Description:** Send an email notification using Resend. Emails are sent from a verified domain. Perfect for notifications, reminders, alerts, or sending reports.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "to",
    "subject",
    "body"
  ],
  "properties": {
    "to": {
      "type": "string",
      "description": "Recipient email address (required)"
    },
    "body": {
      "type": "string",
      "description": "Email body - plain text or HTML (required)"
    },
    "isHtml": {
      "type": "boolean",
      "description": "Whether body is HTML (default: false, plain text)"
    },
    "subject": {
      "type": "string",
      "description": "Email subject line (required)"
    }
  }
}
```

**Parameters:**
- `to` (string, required): Recipient email address (required)
- `body` (string, required): Email body - plain text or HTML (required)
- `isHtml` (boolean): Whether body is HTML (default: false, plain text)
- `subject` (string, required): Email subject line (required)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Resend email ID"
    },
    "to": {
      "type": "string",
      "description": "Recipient email address"
    },
    "error": {
      "type": "string",
      "description": "Error message if failed"
    },
    "success": {
      "type": "boolean",
      "description": "Whether email was sent successfully"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_send_email
    type: action
    name: "Use send_email"
    tool: "default.send_email"
    input:
      to: "example"
      body: "example"
      isHtml: true
      subject: "example"
```

---

#### `default.rest_production-httpbin-get-getrequest`

**Description:** Returns GET data including args, headers, origin and url

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "param1": {
      "type": "string",
      "description": "Test parameter 1"
    },
    "param2": {
      "type": "string",
      "description": "Test parameter 2"
    }
  },
  "additionalProperties": true
}
```

**Parameters:**
- `param1` (string): Test parameter 1
- `param2` (string): Test parameter 2

**Output Schema:**
```json
{
  "type": "object"
}
```

**Usage Example:**
```yaml
steps:
  - id: use_rest_production_httpbin_get_getrequest
    type: action
    name: "Use rest_production-httpbin-get-getrequest"
    tool: "default.rest_production-httpbin-get-getrequest"
    input:
      param1: "example"
      param2: "example"
```

---

#### `default.convert_units`

**Description:** Convert between different units of measurement. Supports weight (kg, lbs, oz, g), length (cm, in, m, ft, km, mi, mm), and temperature (c, f, k).

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "value",
    "from",
    "to"
  ],
  "properties": {
    "to": {
      "enum": [
        "kg",
        "lbs",
        "oz",
        "g",
        "cm",
        "in",
        "m",
        "ft",
        "km",
        "mi",
        "mm",
        "c",
        "f",
        "k"
      ],
      "type": "string",
      "description": "Target unit (kg, lbs, oz, g, cm, in, m, ft, km, mi, mm, c, f, k)"
    },
    "from": {
      "enum": [
        "kg",
        "lbs",
        "oz",
        "g",
        "cm",
        "in",
        "m",
        "ft",
        "km",
        "mi",
        "mm",
        "c",
        "f",
        "k"
      ],
      "type": "string",
      "description": "Source unit (kg, lbs, oz, g, cm, in, m, ft, km, mi, mm, c, f, k)"
    },
    "value": {
      "type": "number",
      "description": "Value to convert"
    },
    "category": {
      "enum": [
        "weight",
        "length",
        "temperature"
      ],
      "type": "string",
      "description": "Optional: category hint for the conversion"
    }
  }
}
```

**Parameters:**
- `to` (string, required): Target unit (kg, lbs, oz, g, cm, in, m, ft, km, mi, mm, c, f, k)
- `from` (string, required): Source unit (kg, lbs, oz, g, cm, in, m, ft, km, mi, mm, c, f, k)
- `value` (number, required): Value to convert
- `category` (string): Optional: category hint for the conversion

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "to": {
      "type": "string",
      "description": "Target unit"
    },
    "from": {
      "type": "string",
      "description": "Source unit"
    },
    "value": {
      "type": "number",
      "description": "Original value"
    },
    "result": {
      "type": "number",
      "description": "Converted value"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_convert_units
    type: action
    name: "Use convert_units"
    tool: "default.convert_units"
    input:
      to: "example"
      from: "example"
      value: 123
      category: "example"
```

---

#### `default.sleep_calculator`

**Description:** Calculate optimal sleep and wake times based on sleep cycles. Supports three modes: sleepNow (when to wake if sleeping now), wakeAt (when to sleep for target wake time), sleepAt (when to wake for target sleep time). Adjusts for different age groups.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "calculatorMode"
  ],
  "properties": {
    "ageGroup": {
      "enum": [
        "adult",
        "teen",
        "child",
        "toddler",
        "infant"
      ],
      "type": "string",
      "description": "Age group for sleep recommendations (default: adult)"
    },
    "targetTime": {
      "type": "string",
      "description": "Target time in HH:MM format (required for wakeAt and sleepAt modes)"
    },
    "calculatorMode": {
      "enum": [
        "sleepNow",
        "wakeAt",
        "sleepAt"
      ],
      "type": "string",
      "description": "sleepNow: calculate wake times from now. wakeAt: calculate sleep times for target wake. sleepAt: calculate wake times for target sleep."
    }
  }
}
```

**Parameters:**
- `ageGroup` (string): Age group for sleep recommendations (default: adult)
- `targetTime` (string): Target time in HH:MM format (required for wakeAt and sleepAt modes)
- `calculatorMode` (string, required): sleepNow: calculate wake times from now. wakeAt: calculate sleep times for target wake. sleepAt: calculate wake times for target sleep.

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "description": "Calculator mode used"
    },
    "results": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Array of sleep/wake time options with cycles and quality"
    },
    "ageGroup": {
      "type": "string",
      "description": "Age group used"
    },
    "inputTime": {
      "type": "string",
      "description": "Input time if provided"
    },
    "recommendation": {
      "type": "object",
      "description": "Sleep recommendation for age group (min, max, optimal hours)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_sleep_calculator
    type: action
    name: "Use sleep_calculator"
    tool: "default.sleep_calculator"
    input:
      ageGroup: "example"
      targetTime: "example"
      calculatorMode: "example"
```

---

#### `default.lucky_number`

**Description:** Generate random lucky number(s) within a range. Default range is 1 to 2,147,483,647. Can generate multiple numbers at once (up to 10).

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "max": {
      "type": "number",
      "description": "Maximum value (default: 2147483647)"
    },
    "min": {
      "type": "number",
      "description": "Minimum value (default: 1)"
    },
    "count": {
      "type": "number",
      "description": "Number of lucky numbers to generate (1-10, default: 1)"
    }
  }
}
```

**Parameters:**
- `max` (number): Maximum value (default: 2147483647)
- `min` (number): Minimum value (default: 1)
- `count` (number): Number of lucky numbers to generate (1-10, default: 1)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "max": {
      "type": "number",
      "description": "Maximum value used"
    },
    "min": {
      "type": "number",
      "description": "Minimum value used"
    },
    "count": {
      "type": "number",
      "description": "Number of numbers generated"
    },
    "range": {
      "type": "string",
      "description": "Human-readable range description"
    },
    "numbers": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "All generated lucky numbers"
    },
    "luckyNumber": {
      "type": "number",
      "description": "The primary lucky number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_lucky_number
    type: action
    name: "Use lucky_number"
    tool: "default.lucky_number"
    input:
      max: 123
      min: 123
      count: 123
```

---

#### `default.calculate_uniqueness`

**Description:** Calculate how unique/rare a person is based on physical characteristics

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "age": {
      "type": "number",
      "description": "Age in years"
    },
    "gender": {
      "enum": [
        "male",
        "female"
      ],
      "type": "string"
    },
    "eyeColor": {
      "enum": [
        "brown",
        "blue",
        "hazel",
        "green",
        "gray",
        "amber"
      ],
      "type": "string"
    },
    "heightCm": {
      "type": "number",
      "description": "Height in centimeters"
    },
    "weightKg": {
      "type": "number",
      "description": "Weight in kilograms"
    },
    "ageMonths": {
      "type": "number",
      "description": "Age in months for babies (0-24)"
    },
    "bloodType": {
      "enum": [
        "O+",
        "O-",
        "A+",
        "A-",
        "B+",
        "B-",
        "AB+",
        "AB-"
      ],
      "type": "string"
    },
    "hairColor": {
      "enum": [
        "black",
        "brown",
        "blonde",
        "red",
        "gray",
        "auburn"
      ],
      "type": "string"
    },
    "handedness": {
      "enum": [
        "right",
        "left",
        "ambidextrous"
      ],
      "type": "string"
    }
  }
}
```

**Parameters:**
- `age` (number): Age in years
- `gender` (string): 
- `eyeColor` (string): 
- `heightCm` (number): Height in centimeters
- `weightKg` (number): Weight in kilograms
- `ageMonths` (number): Age in months for babies (0-24)
- `bloodType` (string): 
- `hairColor` (string): 
- `handedness` (string): 

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "rarity": {
      "type": "string"
    },
    "isBabyMode": {
      "type": "boolean"
    },
    "matchingPeople": {
      "type": "number"
    },
    "worldPopulation": {
      "type": "number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_uniqueness
    type: action
    name: "Use calculate_uniqueness"
    tool: "default.calculate_uniqueness"
    input:
      age: 123
      gender: "example"
      eyeColor: "example"
      heightCm: 123
      weightKg: 123
      ageMonths: 123
      bloodType: "example"
      hairColor: "example"
      handedness: "example"
```

---

#### `default.vibe_quiz`

**Description:** Cat vs Dog personality quiz. Present these 10 questions to the user one by one, collect all answers, then call this tool with the answers array.

QUESTIONS (A=cat-leaning, B=dog-leaning):
1. How do you prefer to spend a Saturday? A: Cozy at home with a book or movie 📚 | B: Out and about, exploring or socializing 🎉
2. When meeting new people, you are: A: Reserved at first, warm up slowly 🤔 | B: Friendly and open right away 😄
3. Your ideal living space is: A: Clean, organized, minimal ✨ | B: Lived-in, cozy, a bit messy is fine 🏠
4. How do you handle stress? A: Need alone time to recharge 🧘 | B: Talk it out with friends/family 💬
5. Your approach to exercise: A: Solo activities (yoga, gym, walks) 🚶 | B: Team sports or group activities ⚽
6. When it comes to routines: A: I like flexibility and doing things my way 🎨 | B: I thrive on consistent schedules 📅
7. Your communication style: A: Subtle hints and body language 👀 | B: Direct and expressive 🗣️
8. How do you show affection? A: Quality time, being present 💝 | B: Physical touch, hugs, enthusiasm 🤗
9. Your sleep preference: A: Night owl, love late nights 🌙 | B: Early bird, up with the sun 🌅
10. When someone annoys you: A: Give them the cold shoulder ❄️ | B: Confront them directly 🔥

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "answers"
  ],
  "properties": {
    "answers": {
      "type": "array",
      "items": {
        "enum": [
          "A",
          "B"
        ],
        "type": "string"
      },
      "description": "Array of exactly 10 answers (A or B) corresponding to each question above"
    }
  }
}
```

**Parameters:**
- `answers` (array, required): Array of exactly 10 answers (A or B) corresponding to each question above

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "type": {
      "enum": [
        "cat",
        "dog"
      ],
      "type": "string",
      "description": "Personality type result"
    },
    "emoji": {
      "type": "string",
      "description": "Result emoji"
    },
    "title": {
      "type": "string",
      "description": "Result title (e.g., \"Total Cat Person!\")"
    },
    "catScore": {
      "type": "number",
      "description": "Number of cat-leaning answers"
    },
    "dogScore": {
      "type": "number",
      "description": "Number of dog-leaning answers"
    },
    "percentage": {
      "type": "number",
      "description": "Match percentage (0-100)"
    },
    "description": {
      "type": "string",
      "description": "Detailed personality description"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_vibe_quiz
    type: action
    name: "Use vibe_quiz"
    tool: "default.vibe_quiz"
    input:
      answers: []
```

---

#### `default.zodiac_compatibility`

**Description:** Check zodiac compatibility between two people. Provide either sign names or birth dates for each person.

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "date1": {
      "type": "string",
      "description": "First person birth date (YYYY-MM-DD) - alternative to sign1"
    },
    "date2": {
      "type": "string",
      "description": "Second person birth date (YYYY-MM-DD) - alternative to sign2"
    },
    "sign1": {
      "enum": [
        "aries",
        "taurus",
        "gemini",
        "cancer",
        "leo",
        "virgo",
        "libra",
        "scorpio",
        "sagittarius",
        "capricorn",
        "aquarius",
        "pisces"
      ],
      "type": "string",
      "description": "First zodiac sign name"
    },
    "sign2": {
      "enum": [
        "aries",
        "taurus",
        "gemini",
        "cancer",
        "leo",
        "virgo",
        "libra",
        "scorpio",
        "sagittarius",
        "capricorn",
        "aquarius",
        "pisces"
      ],
      "type": "string",
      "description": "Second zodiac sign name"
    }
  }
}
```

**Parameters:**
- `date1` (string): First person birth date (YYYY-MM-DD) - alternative to sign1
- `date2` (string): Second person birth date (YYYY-MM-DD) - alternative to sign2
- `sign1` (string): First zodiac sign name
- `sign2` (string): Second zodiac sign name

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "level": {
      "type": "string"
    },
    "person1": {
      "type": "object"
    },
    "person2": {
      "type": "object"
    },
    "compatibility": {
      "type": "number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_zodiac_compatibility
    type: action
    name: "Use zodiac_compatibility"
    tool: "default.zodiac_compatibility"
    input:
      date1: "example"
      date2: "example"
      sign1: "example"
      sign2: "example"
```

---

#### `default.generate_names`

**Description:** Generate random names or numbers. Supports two modes: "names" for generating human names (first, full, or fantasy) and pet names (dog, cat, or other), or "numbers" for generating random numbers within a range.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "properties": {
    "max": {
      "type": "number",
      "description": "Maximum value for random numbers (only for numbers mode, default: 100)"
    },
    "min": {
      "type": "number",
      "description": "Minimum value for random numbers (only for numbers mode, default: 1)"
    },
    "mode": {
      "enum": [
        "names",
        "numbers"
      ],
      "type": "string",
      "description": "Generation mode: \"names\" for name generation, \"numbers\" for random number generation"
    },
    "count": {
      "type": "number",
      "description": "Number of names or numbers to generate (1-100, default: 5)"
    },
    "gender": {
      "enum": [
        "any",
        "male",
        "female"
      ],
      "type": "string",
      "description": "Gender preference for names. \"any\" = random mix, \"male\" = masculine names, \"female\" = feminine names. For pets: male=boy, female=girl"
    },
    "petType": {
      "enum": [
        "dog",
        "cat",
        "other"
      ],
      "type": "string",
      "description": "Type of pet (only when nameCategory is \"pet\"). \"dog\" = dog names, \"cat\" = cat names, \"other\" = hamster/rabbit/etc names"
    },
    "nameCategory": {
      "enum": [
        "human",
        "pet"
      ],
      "type": "string",
      "description": "Category of names to generate (only for names mode). \"human\" for people names, \"pet\" for animal names"
    },
    "humanNameType": {
      "enum": [
        "first",
        "full",
        "fantasy"
      ],
      "type": "string",
      "description": "Type of human name (only when nameCategory is \"human\"). \"first\" = first name only, \"full\" = first + last name, \"fantasy\" = fantasy/fictional names"
    }
  }
}
```

**Parameters:**
- `max` (number): Maximum value for random numbers (only for numbers mode, default: 100)
- `min` (number): Minimum value for random numbers (only for numbers mode, default: 1)
- `mode` (string, required): Generation mode: "names" for name generation, "numbers" for random number generation
- `count` (number): Number of names or numbers to generate (1-100, default: 5)
- `gender` (string): Gender preference for names. "any" = random mix, "male" = masculine names, "female" = feminine names. For pets: male=boy, female=girl
- `petType` (string): Type of pet (only when nameCategory is "pet"). "dog" = dog names, "cat" = cat names, "other" = hamster/rabbit/etc names
- `nameCategory` (string): Category of names to generate (only for names mode). "human" for people names, "pet" for animal names
- `humanNameType` (string): Type of human name (only when nameCategory is "human"). "first" = first name only, "full" = first + last name, "fantasy" = fantasy/fictional names

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "max": {
      "type": "number",
      "description": "Maximum value (numbers mode only)"
    },
    "min": {
      "type": "number",
      "description": "Minimum value (numbers mode only)"
    },
    "mode": {
      "enum": [
        "names",
        "numbers"
      ],
      "type": "string",
      "description": "The generation mode used"
    },
    "count": {
      "type": "number",
      "description": "Number of results generated"
    },
    "range": {
      "type": "string",
      "description": "Range string like \"1 - 100\" (numbers mode only)"
    },
    "gender": {
      "enum": [
        "any",
        "male",
        "female"
      ],
      "type": "string",
      "description": "Gender used for generation"
    },
    "petType": {
      "enum": [
        "dog",
        "cat",
        "other"
      ],
      "type": "string",
      "description": "Type of pet (names mode, pet category only)"
    },
    "results": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of generated names or numbers"
    },
    "nameCategory": {
      "enum": [
        "human",
        "pet"
      ],
      "type": "string",
      "description": "Category of names (names mode only)"
    },
    "humanNameType": {
      "enum": [
        "first",
        "full",
        "fantasy"
      ],
      "type": "string",
      "description": "Type of human name (names mode, human category only)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_generate_names
    type: action
    name: "Use generate_names"
    tool: "default.generate_names"
    input:
      max: 123
      min: 123
      mode: "example"
      count: 123
      gender: "example"
      petType: "example"
      nameCategory: "example"
      humanNameType: "example"
```

---

#### `default.generate_weight_loss_plan`

**Description:** Generate a complete weight loss plan with calorie targets and fasting recommendations

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "age",
    "sex",
    "height",
    "currentWeight",
    "desiredWeight"
  ],
  "properties": {
    "age": {
      "type": "number",
      "description": "Age in years"
    },
    "sex": {
      "enum": [
        "male",
        "female",
        "other"
      ],
      "type": "string"
    },
    "height": {
      "type": "number",
      "description": "Height in centimeters"
    },
    "timeToWeight": {
      "type": "number",
      "description": "Weeks to reach goal (optional)"
    },
    "activityLevel": {
      "enum": [
        "sedentary",
        "light",
        "moderate",
        "active",
        "veryActive"
      ],
      "type": "string"
    },
    "currentWeight": {
      "type": "number",
      "description": "Current weight in kg"
    },
    "desiredWeight": {
      "type": "number",
      "description": "Target weight in kg"
    }
  }
}
```

**Parameters:**
- `age` (number, required): Age in years
- `sex` (string, required): 
- `height` (number, required): Height in centimeters
- `timeToWeight` (number): Weeks to reach goal (optional)
- `activityLevel` (string): 
- `currentWeight` (number, required): Current weight in kg
- `desiredWeight` (number, required): Target weight in kg

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "bmr": {
      "type": "number"
    },
    "tdee": {
      "type": "number"
    },
    "weeksToGoal": {
      "type": "number"
    },
    "targetWeight": {
      "type": "number"
    },
    "currentWeight": {
      "type": "number"
    },
    "dailyCalories": {
      "type": "number"
    },
    "weeklyWeightLoss": {
      "type": "number"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_generate_weight_loss_plan
    type: action
    name: "Use generate_weight_loss_plan"
    tool: "default.generate_weight_loss_plan"
    input:
      age: 123
      sex: "example"
      height: 123
      timeToWeight: 123
      activityLevel: "example"
      currentWeight: 123
      desiredWeight: 123
```

---

#### `default.blood_calculator`

**Description:** Blood calculator with three modes: "donation" (check donation eligibility), "compatibility" (blood type transfusion compatibility), "baby" (predict baby blood type from parents). Mode determines required fields - tool will error with missing fields for each mode.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "calculatorMode"
  ],
  "properties": {
    "age": {
      "type": "number",
      "description": "Age in years (donation mode)"
    },
    "gender": {
      "enum": [
        "male",
        "female"
      ],
      "type": "string",
      "description": "Gender for blood volume calculation (donation mode)"
    },
    "height": {
      "type": "number",
      "description": "Height in cm (metric only) (donation mode)"
    },
    "weight": {
      "type": "number",
      "description": "Weight in kg (metric) or lbs (imperial) (donation mode)"
    },
    "fatherRh": {
      "enum": [
        "+",
        "-"
      ],
      "type": "string",
      "description": "Father's Rh factor (baby mode)"
    },
    "motherRh": {
      "enum": [
        "+",
        "-"
      ],
      "type": "string",
      "description": "Mother's Rh factor (baby mode)"
    },
    "rhFactor": {
      "enum": [
        "+",
        "-"
      ],
      "type": "string",
      "description": "Rh factor positive or negative (compatibility mode)"
    },
    "bloodType": {
      "enum": [
        "A",
        "B",
        "AB",
        "O"
      ],
      "type": "string",
      "description": "ABO blood type (compatibility mode)"
    },
    "heightFeet": {
      "type": "number",
      "description": "Height feet component (imperial only) (donation mode)"
    },
    "unitSystem": {
      "enum": [
        "metric",
        "imperial"
      ],
      "type": "string",
      "description": "Unit system. Default: metric (donation mode)"
    },
    "heightInches": {
      "type": "number",
      "description": "Height inches component (imperial only) (donation mode)"
    },
    "calculatorMode": {
      "enum": [
        "donation",
        "compatibility",
        "baby"
      ],
      "type": "string",
      "description": "Calculator mode: \"donation\" for eligibility, \"compatibility\" for transfusion matching, \"baby\" for predicting baby blood type"
    },
    "fatherBloodType": {
      "enum": [
        "A",
        "B",
        "AB",
        "O"
      ],
      "type": "string",
      "description": "Father's ABO blood type (baby mode)"
    },
    "motherBloodType": {
      "enum": [
        "A",
        "B",
        "AB",
        "O"
      ],
      "type": "string",
      "description": "Mother's ABO blood type (baby mode)"
    }
  }
}
```

**Parameters:**
- `age` (number): Age in years (donation mode)
- `gender` (string): Gender for blood volume calculation (donation mode)
- `height` (number): Height in cm (metric only) (donation mode)
- `weight` (number): Weight in kg (metric) or lbs (imperial) (donation mode)
- `fatherRh` (string): Father's Rh factor (baby mode)
- `motherRh` (string): Mother's Rh factor (baby mode)
- `rhFactor` (string): Rh factor positive or negative (compatibility mode)
- `bloodType` (string): ABO blood type (compatibility mode)
- `heightFeet` (number): Height feet component (imperial only) (donation mode)
- `unitSystem` (string): Unit system. Default: metric (donation mode)
- `heightInches` (number): Height inches component (imperial only) (donation mode)
- `calculatorMode` (string, required): Calculator mode: "donation" for eligibility, "compatibility" for transfusion matching, "baby" for predicting baby blood type
- `fatherBloodType` (string): Father's ABO blood type (baby mode)
- `motherBloodType` (string): Mother's ABO blood type (baby mode)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "tips": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "amount": {
      "type": "number"
    },
    "eligible": {
      "type": "boolean"
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rhWarning": {
      "type": "string"
    },
    "bloodVolume": {
      "type": "number"
    },
    "canDonateTo": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "fullBloodType": {
      "type": "string"
    },
    "maxSafeAmount": {
      "type": "number"
    },
    "possibleTypes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string"
          },
          "percentage": {
            "type": "number"
          }
        }
      }
    },
    "calculatorMode": {
      "type": "string"
    },
    "canReceiveFrom": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "isUniversalDonor": {
      "type": "boolean"
    },
    "isUniversalRecipient": {
      "type": "boolean"
    },
    "rhIncompatibilityRisk": {
      "type": "boolean"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_blood_calculator
    type: action
    name: "Use blood_calculator"
    tool: "default.blood_calculator"
    input:
      age: 123
      gender: "example"
      height: 123
      weight: 123
      fatherRh: "example"
      motherRh: "example"
      rhFactor: "example"
      bloodType: "example"
      heightFeet: 123
      unitSystem: "example"
      heightInches: 123
      calculatorMode: "example"
      fatherBloodType: "example"
      motherBloodType: "example"
```

---

#### `default.calculate_percentage`

**Description:** Calculate percentages with 5 operations: whatIsXPercentOfY (X% of Y), xIsWhatPercentOfY (X is what % of Y), increaseByPercent (Y + X%), decreaseByPercent (Y - X%), percentChange (change from X to Y as %).

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "operation",
    "value1",
    "value2"
  ],
  "properties": {
    "value1": {
      "type": "number",
      "description": "First value: percentage for whatIs/increase/decrease, or base value for percentOf/percentChange"
    },
    "value2": {
      "type": "number",
      "description": "Second value: base value for whatIs/increase/decrease, or total for percentOf, or new value for percentChange"
    },
    "operation": {
      "enum": [
        "whatIsXPercentOfY",
        "xIsWhatPercentOfY",
        "increaseByPercent",
        "decreaseByPercent",
        "percentChange"
      ],
      "type": "string",
      "description": "Operation: whatIsXPercentOfY (X% of Y), xIsWhatPercentOfY (X is what % of Y), increaseByPercent (Y increased by X%), decreaseByPercent (Y decreased by X%), percentChange (% change from X to Y)"
    }
  }
}
```

**Parameters:**
- `value1` (number, required): First value: percentage for whatIs/increase/decrease, or base value for percentOf/percentChange
- `value2` (number, required): Second value: base value for whatIs/increase/decrease, or total for percentOf, or new value for percentChange
- `operation` (string, required): Operation: whatIsXPercentOfY (X% of Y), xIsWhatPercentOfY (X is what % of Y), increaseByPercent (Y increased by X%), decreaseByPercent (Y decreased by X%), percentChange (% change from X to Y)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "result": {
      "type": "number",
      "description": "The calculated result"
    },
    "value1": {
      "type": "number",
      "description": "First input value"
    },
    "value2": {
      "type": "number",
      "description": "Second input value"
    },
    "operation": {
      "type": "string",
      "description": "The operation performed"
    },
    "explanation": {
      "type": "string",
      "description": "Human-readable explanation of the calculation"
    },
    "resultIsPercent": {
      "type": "boolean",
      "description": "Whether the result is a percentage"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_percentage
    type: action
    name: "Use calculate_percentage"
    tool: "default.calculate_percentage"
    input:
      value1: 123
      value2: 123
      operation: "example"
```

---

#### `default.spin_wheel`

**Description:** Spin a wheel to randomly select from custom options. Great for making decisions, picking winners, or choosing randomly between choices. Requires at least 2 options.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "options"
  ],
  "properties": {
    "options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of options to spin between (minimum 2 required). Examples: [\"Pizza\", \"Burger\", \"Sushi\"] or [\"Yes\", \"No\", \"Maybe\"]"
    }
  }
}
```

**Parameters:**
- `options` (array, required): List of options to spin between (minimum 2 required). Examples: ["Pizza", "Burger", "Sushi"] or ["Yes", "No", "Maybe"]

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "index": {
      "type": "number",
      "description": "Index of the winning option (0-based)"
    },
    "result": {
      "type": "string",
      "description": "The winning option selected by the wheel"
    },
    "options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "All options that were in the wheel"
    },
    "segmentAngle": {
      "type": "number",
      "description": "Angle of each segment in degrees"
    },
    "totalOptions": {
      "type": "number",
      "description": "Total number of options in the wheel"
    },
    "finalRotation": {
      "type": "number",
      "description": "Final rotation angle in degrees (for animation)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_spin_wheel
    type: action
    name: "Use spin_wheel"
    tool: "default.spin_wheel"
    input:
      options: []
```

---

#### `default.make_decision`

**Description:** Help make a decision. Supports three modes: "yesNo" for yes/no questions (no options needed), "pickOne" for random selection from options, and "weighted" for weighted random selection. Great for making choices, answering questions, or picking randomly.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "properties": {
    "mode": {
      "enum": [
        "yesNo",
        "pickOne",
        "weighted"
      ],
      "type": "string",
      "description": "Decision mode: \"yesNo\" for yes/no questions, \"pickOne\" for random selection, \"weighted\" for weighted selection"
    },
    "options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Options to choose from (required for pickOne and weighted modes). Example: [\"Pizza\", \"Burger\", \"Sushi\"]"
    },
    "weights": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Optional weights for each option (only used in weighted mode). Higher weight = higher chance. Example: [3, 2, 1]"
    }
  }
}
```

**Parameters:**
- `mode` (string, required): Decision mode: "yesNo" for yes/no questions, "pickOne" for random selection, "weighted" for weighted selection
- `options` (array): Options to choose from (required for pickOne and weighted modes). Example: ["Pizza", "Burger", "Sushi"]
- `weights` (array): Optional weights for each option (only used in weighted mode). Higher weight = higher chance. Example: [3, 2, 1]

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "icon": {
      "type": "string",
      "description": "Emoji icon for the result"
    },
    "mode": {
      "type": "string",
      "description": "The mode used for the decision"
    },
    "index": {
      "type": "number",
      "description": "Index of selected option (for pickOne/weighted modes)"
    },
    "options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "All options that were considered"
    },
    "decision": {
      "type": "string",
      "description": "The decision result"
    },
    "confidence": {
      "type": "number",
      "description": "Confidence level (0-100)"
    },
    "totalOptions": {
      "type": "number",
      "description": "Total number of options considered"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_make_decision
    type: action
    name: "Use make_decision"
    tool: "default.make_decision"
    input:
      mode: "example"
      options: []
      weights: []
```

---

#### `default.flip_tool`

**Description:** Flip coins or roll dice. Use flipMode to select: "coin" for coin flips (heads/tails), "dice" for dice rolls.

**Input Schema:**
```json
{
  "type": "object",
  "required": [],
  "properties": {
    "count": {
      "type": "number",
      "description": "Number of coins to flip or dice to roll (default: 1, max: 6 for dice, 100 for coins)"
    },
    "sides": {
      "type": "number",
      "description": "Number of sides on dice (default: 6, only used in dice mode)"
    },
    "flipMode": {
      "enum": [
        "coin",
        "dice"
      ],
      "type": "string",
      "description": "Mode: \"coin\" for coin flip, \"dice\" for dice roll (default: coin)"
    }
  }
}
```

**Parameters:**
- `count` (number): Number of coins to flip or dice to roll (default: 1, max: 6 for dice, 100 for coins)
- `sides` (number): Number of sides on dice (default: 6, only used in dice mode)
- `flipMode` (string): Mode: "coin" for coin flip, "dice" for dice roll (default: coin)

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "count": {
      "type": "number",
      "description": "Number of coins/dice used"
    },
    "rolls": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "All dice roll results"
    },
    "sides": {
      "type": "number",
      "description": "Number of sides on dice"
    },
    "total": {
      "type": "number",
      "description": "Sum of all dice rolls"
    },
    "result": {
      "type": "string",
      "description": "Single coin result (heads/tails)"
    },
    "results": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "All coin flip results"
    },
    "flipMode": {
      "type": "string",
      "description": "Mode used: coin or dice"
    },
    "headsCount": {
      "type": "number",
      "description": "Number of heads (coin mode)"
    },
    "tailsCount": {
      "type": "number",
      "description": "Number of tails (coin mode)"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_flip_tool
    type: action
    name: "Use flip_tool"
    tool: "default.flip_tool"
    input:
      count: 123
      sides: 123
      flipMode: "example"
```

---

#### `default.calculate_cycle`

**Description:** Calculate menstrual cycle predictions including next period date, fertile window, ovulation date, and current cycle phase. Supports both simplified mode (average 28-day cycle) and advanced mode with custom cycle/period lengths.

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "date"
  ],
  "properties": {
    "date": {
      "type": "string",
      "description": "Date of period in YYYY-MM-DD format. Example: \"2026-01-01\""
    },
    "isFirstDay": {
      "type": "boolean",
      "description": "If true (default), date is first day of period. If false, date is last day of bleeding."
    },
    "simplified": {
      "type": "boolean",
      "description": "If true, use simplified mode with average 28-day cycle and 5-day period. Ignores cycleLength and periodLength."
    },
    "cycleLength": {
      "type": "number",
      "description": "Average cycle length in days (default: 28). Only used when simplified is false."
    },
    "periodLength": {
      "type": "number",
      "description": "Average period length in days (default: 5). Only used when simplified is false."
    }
  }
}
```

**Parameters:**
- `date` (string, required): Date of period in YYYY-MM-DD format. Example: "2026-01-01"
- `isFirstDay` (boolean): If true (default), date is first day of period. If false, date is last day of bleeding.
- `simplified` (boolean): If true, use simplified mode with average 28-day cycle and 5-day period. Ignores cycleLength and periodLength.
- `cycleLength` (number): Average cycle length in days (default: 28). Only used when simplified is false.
- `periodLength` (number): Average period length in days (default: 5). Only used when simplified is false.

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "description": "\"simplified\" or \"advanced\""
    },
    "phase": {
      "type": "string",
      "description": "Current phase: menstrual, follicular, ovulation, or luteal"
    },
    "phaseInfo": {
      "type": "object",
      "description": "Phase details with name, emoji, color, and description"
    },
    "currentDay": {
      "type": "number",
      "description": "Current day in the cycle (1-based)"
    },
    "cycleLength": {
      "type": "number",
      "description": "Cycle length used for calculation"
    },
    "periodLength": {
      "type": "number",
      "description": "Period length used for calculation"
    },
    "nextPeriodEnd": {
      "type": "string",
      "description": "Next period end date (YYYY-MM-DD)"
    },
    "ovulationDate": {
      "type": "string",
      "description": "Predicted ovulation date (YYYY-MM-DD)"
    },
    "nextPeriodStart": {
      "type": "string",
      "description": "Next period start date (YYYY-MM-DD)"
    },
    "fertileWindowEnd": {
      "type": "string",
      "description": "Fertile window end date (YYYY-MM-DD)"
    },
    "fertileWindowStart": {
      "type": "string",
      "description": "Fertile window start date (YYYY-MM-DD)"
    },
    "daysUntilNextPeriod": {
      "type": "number",
      "description": "Days until next period starts"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_cycle
    type: action
    name: "Use calculate_cycle"
    tool: "default.calculate_cycle"
    input:
      date: "example"
      isFirstDay: true
      simplified: true
      cycleLength: 123
      periodLength: 123
```

---

#### `default.calculate_iq_score`

**Description:** IQ assessment quiz. Present questions to user one by one, collect all answers (0-3 index), then call with answers array.

TEST MODES:
- quick: 15 questions (~5 min) - Questions 1-15 below
- standard: 30 questions (~12 min) - Questions 1-30 below
- comprehensive: 50 questions (~20 min) - All 50 questions

QUESTIONS (answer index 0-3 for options A-D):

PATTERN: 1. 2,4,8,16,? [24|32|30|20] 2. 3,6,11,18,? [25|27|26|24] 3. 1,1,2,3,5,8,? [11|12|13|15] 4. 81,27,9,3,? [0|1|2|6] 5. If 2=6,3=12,4=20, 5=? [25|30|35|40] 6. 1,4,9,16,25,? [30|36|49|35] 7. 2,6,12,20,30,? [40|42|44|38] 8. 1,2,4,7,11,16,? [20|21|22|23] 9. 3,5,9,17,33,? [49|57|65|66] 10. 1,3,6,10,15,21,? [25|27|28|30]

LOGIC: 11. All Bloops are Razzies, all Razzies are Lazzies. All Bloops are Lazzies? [True|False|Cannot determine|Sometimes] 12. 5 machines make 5 widgets in 5 min. 100 machines for 100 widgets? [100min|5min|20min|1min] 13. Bat+ball=$1.10, bat costs $1 more. Ball cost? [$0.10|$0.05|$0.15|$0.20] 14. Mary's father has 5 daughters: Nana,Nene,Nini,Nono. 5th name? [Nunu|Mary|Nana|None] 15. Some cats are dogs, some dogs are birds. Can cats be birds? [Yes|No|Possibly|Sometimes] 16. Farmer has 17 sheep, all but 9 die. Left? [8|9|17|0] 17. 3 people dig 3 holes in 3 hours. 1 person dig half hole? [1hr|1.5hr|3hr|Impossible] 18. Take pill every 30min, 3 pills. How long? [1.5hr|1hr|2hr|30min] 19. Subtract 5 from 25 how many times? [5|1|4|Infinite] 20. 6 apples, take 4. How many do you have? [2|4|6|0]

MATH: 21. 15% of 200? [25|30|35|40] 22. x+5=12, x=? [5|6|7|8] 23. 144÷12? [10|11|12|14] 24. √169? [11|12|13|14] 25. 3x-7=14, x=? [5|6|7|8] 26. 25% of 80? [15|20|25|30] 27. 7×8+6÷2? [56|59|31|62] 28. 120km in 2hr, speed? [50|55|60|65] 29. 2³+3²? [13|15|17|19] 30. Boys:girls=3:5, 24 boys. Girls? [30|35|40|45]

SPATIAL: 31. ○□△○□? [○|□|△|◇] 32. Fold square twice, cut corner. Holes? [1|2|4|8] 33. Cube faces? [4|6|8|12] 34. Rotate N 180°? [Z|N|M|W] 35. Cube edges? [6|8|10|12] 36. Most sides: hexagon/pentagon/octagon? [Hexagon|Pentagon|Octagon|Equal] 37. 3:15 clock angle? [0°|7.5°|15°|22.5°] 38. Triangles in Star of David? [2|6|8|12] 39. AMBULANCE mirror image? [AMBULANCE|ECNALUBMA|Reversed|Upside down] 40. Tetrahedron vertices? [3|4|5|6]

VERBAL: 41. HAND:GLOVE as FOOT:? [Leg|Sock|Shoe|Toe] 42. CIFAIPC rearranged=? [City|Animal|Ocean|Country] 43. BOOK:READING as FORK:? [Drawing|Eating|Writing|Cooking] 44. Doesn't belong: Apple,Banana,Carrot,Orange? [Apple|Banana|Carrot|Orange] 45. DOCTOR:HOSPITAL as TEACHER:? [Student|School|Book|Classroom] 46. Opposite of BENEVOLENT? [Kind|Malevolent|Generous|Helpful] 47. BIRD:NEST as BEE:? [Honey|Flower|Hive|Sting] 48. EALGER rearranged=? [Bird|Color|Country|Fruit] 49. EPHEMERAL means? [Eternal|Temporary|Solid|Ancient] 50. WATER:THIRST as FOOD:? [Eat|Hunger|Cook|Taste]

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "testMode",
    "answers"
  ],
  "properties": {
    "answers": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Array of answer indices (0-3) for each question. Length must match testMode: 15 for quick, 30 for standard, 50 for comprehensive."
    },
    "testMode": {
      "enum": [
        "quick",
        "standard",
        "comprehensive"
      ],
      "type": "string",
      "description": "Test mode determining which questions to use. quick=Q1-15, standard=Q1-30, comprehensive=Q1-50"
    }
  }
}
```

**Parameters:**
- `answers` (array, required): Array of answer indices (0-3) for each question. Length must match testMode: 15 for quick, 30 for standard, 50 for comprehensive.
- `testMode` (string, required): Test mode determining which questions to use. quick=Q1-15, standard=Q1-30, comprehensive=Q1-50

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "emoji": {
      "type": "string",
      "description": "Result emoji"
    },
    "iqScore": {
      "type": "number",
      "description": "Estimated IQ score"
    },
    "accuracy": {
      "type": "number",
      "description": "Accuracy percentage"
    },
    "category": {
      "type": "string",
      "description": "IQ category (e.g., \"Superior\", \"Average\")"
    },
    "testInfo": {
      "type": "object",
      "description": "Test configuration (name, questionCount, estimatedMinutes)"
    },
    "testMode": {
      "type": "string",
      "description": "Test mode used"
    },
    "percentile": {
      "type": "number",
      "description": "Percentile ranking (0-100)"
    },
    "categoryScores": {
      "type": "object",
      "description": "Breakdown by category (pattern, logic, math, spatial, verbal)"
    },
    "correctAnswers": {
      "type": "number",
      "description": "Number of correct answers"
    },
    "totalQuestions": {
      "type": "number",
      "description": "Total questions in test"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_iq_score
    type: action
    name: "Use calculate_iq_score"
    tool: "default.calculate_iq_score"
    input:
      answers: []
      testMode: "example"
```

---

#### `default.calculate_ideal_weight`

**Description:** Calculate ideal weight using the Devine formula

**Input Schema:**
```json
{
  "type": "object",
  "required": [
    "height",
    "sex"
  ],
  "properties": {
    "sex": {
      "enum": [
        "male",
        "female",
        "other"
      ],
      "type": "string",
      "description": "Biological sex"
    },
    "height": {
      "type": "number",
      "description": "Height in centimeters"
    }
  }
}
```

**Parameters:**
- `sex` (string, required): Biological sex
- `height` (number, required): Height in centimeters

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "gender": {
      "type": "string"
    },
    "height": {
      "type": "number"
    },
    "formula": {
      "type": "string",
      "description": "Formula used (Devine)"
    },
    "idealWeight": {
      "type": "number",
      "description": "Ideal weight in kg"
    }
  }
}
```

**Usage Example:**
```yaml
steps:
  - id: use_calculate_ideal_weight
    type: action
    name: "Use calculate_ideal_weight"
    tool: "default.calculate_ideal_weight"
    input:
      sex: "example"
      height: 123
```

---


## Detected Notification Tools

The following notification tools were auto-detected from your connectors:

### 🔔 Push

- `default.send_push_notification` - Send a push notification to the user's registered devices (browser/mobile). Use this to alert users about automation status, required inputs, errors, or any important updates. The notification will appear in the browser or on mobile devices that have enabled push notifications.

### 📧 Email

- `default.send_email` - Send an email notification using Resend. Emails are sent from a verified domain. Perfect for notifications, reminders, alerts, or sending reports.

### Using Notification Tools

These tools are automatically used for:
- **Human-in-the-loop requests**: When an automation needs user input
- **Error notifications**: When an automation fails or requires attention
- **Output delivery**: When configured in the `outputs` section

Example usage in workflow:
```yaml
outputs:
  - type: push
    tool: default.send_push_notification
  - type: email
    tool: default.send_email
    to: "{{user_email}}"
    subject: "Automation Complete"
    body: "{{summary}}"
```

## Available Automations (for trigger_automation)

Use the `trigger_automation` block to trigger other automations from within a workflow.
The `{api_key}` placeholder will be automatically replaced with the user's API key at runtime.

*No automations available yet. Create automations to enable chaining.*
<!-- TOOLS_SECTION_END -->

